# Nhật ký làm việc cá nhân - Nguyễn Đạt Sang

## Em đã làm được những gì trong phần này?
- **Vượt lỗi môi trường máy cá nhân:** Do máy không xài được các thư viện nặng như pandas hay scikit-learn, mình đã soạn toàn bộ bằng code Python (dùng thư viện `csv` có sẵn) để mọi thứ chạy mượt mà không bị lỗi.
- **Phần Khám phá dữ liệu (Góp cho Report 3):** 
  - Đọc file dữ liệu mẫu (`foody_sample_data.csv`).
  - Kiểm tra số lượng dòng, xem trước nội dung review và kiểm tra xem dữ liệu có bị thiếu .
- **Phần Xây dựng mô hình cơ bản (Góp cho Report 4):** 
  - Lọc bỏ các điểm review trung lập, chỉ giữ lại review tích cực và tiêu cực để phân loại.
  - Tạo một mô hình đơn giản dựa trên danh sách từ khóa tiếng Việt (từ tích cực/tiêu cực) để máy tự phán đoán cảm xúc của khách hàng và tính ra độ chính xác (Accuracy).

## Tình trạng hiện tại
- Đã hoàn thành toàn bộ file code trong thư mục cá nhân (`code/sang/`).
- Đã đẩy (push) code lên nhánh riêng (`sang`) trên GitHub của nhóm an toàn, không bị mất mát gì cả.